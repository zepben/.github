name: NPM App Snapshot Release

on: workflow_dispatch

jobs:
  run:
    uses: zepben/.github/.github/workflows/npm-app-snapshot-release.yml@main
    with:
      product-key: "<some-product-key>"   # Must be unique
      product-repo: "<some-product-repo>"
    secrets: inherit
